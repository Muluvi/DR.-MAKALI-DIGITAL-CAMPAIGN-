---
description: Phase 4 — build one of the six Tier 2 scrubbed signature moments. Pass which one.
argument-hint: "[offline-split | four-hour-cycle | closed-loop | tac40 | ussd | map]"
---

Build one Tier 2 signature moment: $ARGUMENTS

There are **six of these in the entire document**. That scarcity is the point —
it is what makes each one register. Before building, confirm how many are
already done. If this would be the seventh, stop and tell me.

## The six, ranked by argumentative value

1. **offline-split** — the 86.4% figure (§8.10.1). The strategic core of the
   entire proposal: a scroll-scrubbed fill resolving 143,340 against 1,053,991.
   This should be the loudest thing on the site. Figures come from the section
   text; use no others.
2. **map** — the Kitui ward map, once §8.10.1's connectivity data drives it.
3. **four-hour-cycle** — §8.8.2. A scrubbed clock advancing T+00:00 → T+04:00
   across the four stages. The component already carries `#cycle-marker-0..3`
   for exactly this.
4. **closed-loop** — §8.8. Intelligence visibly flowing inbound, through the war
   room, back out. Rails already carry stable ids.
5. **tac40** — already the strongest component in the document. Make it the
   showpiece; change no displayed data.
6. **ussd** — make keypresses feel physical, 2G and session states legible at a
   glance. Add no vernacular string that is not already in the file.

## Requirements

- Scrubbed to scroll position, not time-based autoplay.
- Dynamically import whatever timeline library you need. **Report the added
  gzipped bytes.** If it pushes the route over budget, say so before merging.
- Confirm the import does not load on routes that do not use it.
- Complete, legible static end-state with JS off, motion reduced, or
  `Save-Data` on. Verify all three, do not assume.
- Measure INP on the route afterwards with 4× CPU throttling. Scrubbed scroll
  is the classic way to blow an INP budget.

Report: added bytes, measured INP before and after, and the three degraded
states you verified.
